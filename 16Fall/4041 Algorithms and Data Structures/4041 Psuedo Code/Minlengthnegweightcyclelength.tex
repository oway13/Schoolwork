MIN-LENGTH-NEG-CYCLE-LENGTH(W)
	L = SLOW-ALL-PAIRS-SHORTEST-PATHS(W)
	count = 0
	for i = 1 to length(L)
		count++
		if L[i][i] < 0
			return count
	return 0
//Checks along the diagonal of shortest-path weights
//The index of the first negative is the length
//O(n^4) 